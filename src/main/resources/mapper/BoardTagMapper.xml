<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.ggshop.v1.mapper.BoardTagMapper">

    <!-- 태그 등록 -->
    <insert id="insertTag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tbl_board_tag (
            tag_name,
            tag_board_id
        ) VALUES (
                     #{tagName},
                     #{tagBoardId}
                 )
    </insert>

    <!-- 게시글의 태그 목록 조회 -->
    <select id="selectTagsByBoardId" resultType="String">
        SELECT tag_name
        FROM tbl_board_tag
        WHERE tag_board_id = #{boardId}
    </select>

</mapper>